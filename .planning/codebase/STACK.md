# Stack & Dependencies

## Core Technologies
- **Next.js**: 16.1.1 (Configurado com `output: 'export'` no next.config.ts)
- **React/React DOM**: 19.2.3 (App Router com diretório `app/`)
- **TypeScript**: Configuração estrita (v5)

## Styling & Animations
- **Tailwind CSS**: v4 (com PostCSS, paletização definida em `@theme` no arquivo `app/globals.css`)
- **Framer Motion**: ^12.25.0 (Usado massivamente para Page Reveal, Hover effects, UI feedback como `Typewriter`)
- **Lucide React**: ^0.562.0 (Ícones limpos semânticos)

## Utility/Auxiliares
- **Sonner**: Para componentes de Toast leves no submit do form.
- **Node.js**: v20+ Environment expected
